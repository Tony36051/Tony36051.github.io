# 固定使用公网 npm registry，避免本地华为内网镜像源
# (mirrors.tools.huawei.com) 被写入 package-lock.json，导致
# 公网 CI（GitHub Actions）无法访问而 ENOTFOUND。
# 本地内网开发如需用镜像源，请用 --registry 临时覆盖，勿改此文件。
registry=https://registry.npmjs.org/
